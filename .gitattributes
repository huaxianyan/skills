# 统一按 LF 检出，保证脚本在任何平台上的行为一致
* text=auto eol=lf

# Windows 批处理与 PowerShell 脚本保持 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.ico binary
*.zip binary
*.jar binary
